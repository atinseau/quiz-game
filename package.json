{
  "name": "quiz-app",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun --filter '*' dev --parallel",
    "build": "bun --filter '*' build --parallel",
    "fclean": "rm -rf node_modules bun.lock apps/*/node_modules apps/*/dist"
  }
}
