import type { ServerRound, SpecialRoundType } from "./types";

// Populated in Task 3 — stub so framework.ts compiles now.
export const roundRegistry: Partial<Record<SpecialRoundType, ServerRound>> = {};
