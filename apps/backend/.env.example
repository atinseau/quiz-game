HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Database (Strapi)
DATABASE_CLIENT=postgres
DATABASE_URL=postgresql://strapi:strapi@localhost:5432/quiz

# Postgres container (used by docker-compose for local dev)
# Keep POSTGRES_* consistent with DATABASE_URL credentials above.
POSTGRES_USER=strapi
POSTGRES_PASSWORD=strapi
POSTGRES_DB=quiz
# POSTGRES_PORT=5432

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# OpenAI (for question-import plugin)
OPENAI_API_KEY=
